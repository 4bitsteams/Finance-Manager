<UserControl x:Class="FinanceManager.Views.AccountsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FinanceManager.Views" 
             xmlns:viewmodels="clr-namespace:FinanceManager.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type=viewmodels:AccountsViewModel}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            
        </Grid.ColumnDefinitions>
        <StackPanel>
            <TextBlock FontSize="20"
                   Grid.Row="0"
                   VerticalAlignment="Top"
                   HorizontalAlignment="Center" 
                   Text="Total:"></TextBlock>
            <TextBlock FontSize="20"
                   Grid.Row="0"
                   VerticalAlignment="Top"
                   HorizontalAlignment="Center" 
                   Text="{Binding TotalAmount, StringFormat={}{0:C}}"></TextBlock>
        </StackPanel>
        <ListBox Height="Auto" Grid.Row="1" BorderThickness="0" Background="Transparent" ItemsSource="{Binding Accounts}"
                 SelectedItem="{Binding SelectedAccount}" Width="Auto" Margin="5">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="SnapsToDevicePixels"
                    Value="true" />
                    <Setter Property="OverridesDefaultStyle"
                    Value="true" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <Border x:Name="Border"
                                        Padding="2"
                                        CornerRadius="5"
                                        Margin="5"
                                        SnapsToDevicePixels="true">
                                    <Border.Background>
                                        <SolidColorBrush x:Name="BackgroundColor" Color="#b8d3ff" Opacity="0.5" />
                                    </Border.Background>
                                    <VisualStateManager.VisualStateGroups>
                                        <VisualStateGroup x:Name="SelectionStates">
                                            <VisualState x:Name="Unselected" />
                                            <VisualState x:Name="Selected">
                                                <Storyboard>
                                                    <ColorAnimation Storyboard.TargetName="BackgroundColor"
                                                            Storyboard.TargetProperty="Color" To="Red" Duration="0:0:0.01">
                                                    </ColorAnimation>
                                                </Storyboard>
                                            </VisualState>
                                            <VisualState x:Name="SelectedUnfocused">
                                                <Storyboard>
                                                    <ColorAnimation Storyboard.TargetName="BackgroundColor"
                                                            Storyboard.TargetProperty="Color" To="#85a0d7" Duration="0:0:0.01">
                                                    </ColorAnimation>
                                                </Storyboard>
                                            </VisualState>
                                        </VisualStateGroup>
                                    </VisualStateManager.VisualStateGroups>
                                    <ContentPresenter />
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="10">
                        <TextBlock Width="Auto" FontSize="18" Text="{Binding Path=Name}"/>
                    </StackPanel>
                    <DataTemplate.Triggers>
                    </DataTemplate.Triggers>
                </DataTemplate>
            </ListBox.ItemTemplate>
            
        </ListBox>
            
    </Grid>
</UserControl>
